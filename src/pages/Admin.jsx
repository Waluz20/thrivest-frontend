import React,{useEffect,useState} from 'react'; import { allDeposits, approveDeposit } from '../api'
export default function Admin(){ const [list,setList]=useState([]); async function load(){ const d=await allDeposits(); setList(d) } useEffect(()=>{ load() },[]) async function doApprove(id){ await approveDeposit(id); await load() } return (<div className='card'><h3>Admin - All deposits</h3><table className='table'><thead><tr><th>User</th><th>Amount</th><th>Method</th><th>Status</th><th>Action</th></tr></thead><tbody>{list.map(d=>(<tr key={d.id}><td>{d.userName}</td><td>${d.amount}</td><td>{d.method}</td><td><span className='badge'>{d.status}</span></td><td>{d.status==='pending' ? <button className='btn' onClick={()=>doApprove(d.id)}>Approve</button> : '—'}</td></tr>))}</tbody></table><p style={{marginTop:10}}>Note: first signup is admin (for demo)</p></div>) }
